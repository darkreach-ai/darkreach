# Encrypted with SOPS + age. Do not edit directly.
# Use: sops secrets/infra.enc.yaml
#
# To encrypt after first edit:
#   sops --encrypt --in-place secrets/infra.enc.yaml
#
# Contains infrastructure secrets (Hetzner, signing keys, etc.)

hcloud_token: "CHANGE_ME"
